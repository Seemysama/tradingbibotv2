# =============================================================================
# TRADING ENGINE V2 - Requirements GPU (Ubuntu + CUDA)
# =============================================================================
# Installation:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements_gpu.txt
# =============================================================================

# === Core Data Science ===
pandas>=2.0.0
numpy>=1.24.0
polars>=0.19.0           # Alternative rapide à pandas
pyarrow>=14.0.0          # Parquet optimisé

# === ML / Deep Learning ===
# torch installé séparément avec CUDA
scikit-learn>=1.3.0
numba>=0.58.0            # JIT compilation

# === Trading / Exchange ===
ccxt>=4.0.0              # Binance API
websockets>=12.0

# === API Backend ===
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.0.0

# === Visualization ===
matplotlib>=3.8.0
plotly>=5.18.0

# === Utilities ===
tqdm>=4.66.0
python-dateutil>=2.8.2
requests>=2.31.0
aiohttp>=3.9.0

# === Monitoring (optionnel) ===
# tensorboard>=2.15.0
# wandb>=0.16.0

# === Production (optionnel) ===
# gunicorn>=21.2.0
# redis>=5.0.0
